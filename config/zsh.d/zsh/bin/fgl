#!/usr/bin/env bash
set -ue -o pipefail

# Figlet font selector

fgl() (
	cd /usr/local/Cellar/figlet/*/share/figlet/fonts || exit
	ls *.flf | sort | fzf --no-multi --reverse --preview "figlet -f {} Hello World!"
)

fgl "$@"
