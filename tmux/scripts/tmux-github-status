#!/bin/bash
GITHUB_STATUS=$(curl -sS https://status.github.com/api/status.json | jq -r '.status')

case $GITHUB_STATUS in
  ma*)
    SIGNAL="#[fg=red]⨉"
    ;;
  mi*)
    SIGNAL="#[fg=yellow]~"
    ;;
  *)
    SIGNAL="✓"
    ;;
esac

echo "$SIGNAL"

