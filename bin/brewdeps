#!/usr/bin/env zsh

# List all installed brew formulas and its dependencies (so slow! make it fast)

main() {
  brew list | while read cask; do echo -n "$cask -> "; brew deps $cask | awk '{printf(" %s ", $0)}'; echo ""; done
}

main
