#!/usr/bin/env sh

# Simple function to get weather data
#
# Usage:
#   $ weather
#   $ weather <city>

weather () {
  curl -H "Accept-Language: ${LANG%_*}" wttr.in/"${1:-Amsterdam}"?0q
}

weather "$@"
